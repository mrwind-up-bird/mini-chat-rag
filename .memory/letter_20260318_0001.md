# Letter to Myself (Session Handoff)

**Date:** 2026-03-18 ~00:00 UTC

## 1. Executive Summary
* **Goal:** Fix 500 error when creating NyxCore sources via dashboard on production, and update documentation to cover the NyxCore Axiom integration.
* **Current Status:** Bug fixed, deployed, and live on mini-rag.de. Docs updated. All 144 tests passing.

## 2. The "Done" List (Context Anchor)
* **Root cause identified**: PostgreSQL uses a native enum type `sourcetype` for the `source_type` column. The `nyxcore` value was added to the Python `StrEnum` but never added to the PostgreSQL enum type — causing a 500 on INSERT.
* Created `alembic/versions/a1b2c3d4e5f6_add_nyxcore_to_sourcetype_enum.py` — runs `ALTER TYPE sourcetype ADD VALUE IF NOT EXISTS 'nyxcore'`
* Updated `.github/workflows/deploy.yml`:
  - Added `alembic/**` to `paths` filter so migrations trigger deploys
  - Added `docker compose exec -T web alembic upgrade head` step after container rebuild
  - Added `workflow_dispatch` trigger for manual deploys
* Updated `docs/architecture.md`:
  - Added NyxCore Axiom to system overview diagram
  - Added NyxCore participant + flow to RAG pipeline sequence diagram
  - Added NyxCore validation path to ingestion pipeline diagram
  - Updated context injection example with authority labels
  - Added `nyxcore` to SourceType enum table
* Updated `docs/admin-guide.md` — added NyxCore source type (item 4) in Sources section
* Updated `docs/installation.md` — test count 129 → 144
* Updated `CLAUDE.md` — NyxCore in RAG pipeline, ingestion pipeline, and source config encryption pattern

## 3. The "Pain" Log (CRITICAL)
* **Tried:** Creating NyxCore source via dashboard on production (mini-rag.de)
* **Failed:** 500 Internal Server Error — PostgreSQL native enum `sourcetype` didn't include `nyxcore`
* **Root cause:** SQLAlchemy maps Python StrEnum to a native PostgreSQL ENUM type. Adding a new member to the Python enum doesn't update the DB enum — requires an Alembic migration: `ALTER TYPE sourcetype ADD VALUE IF NOT EXISTS 'nyxcore'`
* **Fix:** Created Alembic migration, deployed, ran `alembic upgrade head` on VPS
* *Note:* Tests pass with SQLite (which uses VARCHAR, no enum constraints) so this bug was invisible in the test suite. Always check if StrEnum changes need a migration.
* **Tried:** Deploy workflow didn't trigger on migration-only commits
* **Failed:** `.github/workflows/deploy.yml` `paths` filter didn't include `alembic/**`
* **Fix:** Added `alembic/**` to paths, added `workflow_dispatch` for manual triggers

## 4. Active Variable State
* Commits: `34eda10` (migration + docs), `5573170` (deploy paths), `dc68997` (workflow_dispatch)
* Deploy run: #23221990902 — completed successfully
* Production: mini-rag.de — migration applied, NyxCore source creation should now work
* Previous session checkpoint: `.memory/letter_20260317_0001.md`

## 5. Immediate Next Steps
1. [ ] Verify NyxCore source creation works on mini-rag.de dashboard (the actual end-to-end test)
2. [ ] Verify ingestion (validation) succeeds and source goes to "ready" status
3. [ ] Test chat with NyxCore context — ask a question that should retrieve NyxCore chunks
4. [ ] Consider adding a CI step that checks for enum changes without corresponding migrations
5. [ ] The Caddy vs Traefik discrepancy in docs/installation.md still exists (installation.md references Caddy, but docker-compose.yml uses Traefik labels)
