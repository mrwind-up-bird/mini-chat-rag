# Letter to Myself (Session Handoff)

**Date:** 2026-03-17 ~23:00 UTC

## 1. Executive Summary
* **Goal:** Add NyxCore Axiom as an external RAG source type to MiniRAG, so bots can query NyxCore's hybrid search API at chat time and include results in LLM context.
* **Current Status:** Feature complete, all tests passing (144/144), committed (`c9cbb0f`), pushed to `main`, deployed to VPS via GitHub Actions (succeeded in 15s). Live on mini-rag.de.

## 2. The "Done" List (Context Anchor)
* Added `NYXCORE = "nyxcore"` to `SourceType` enum in `app/models/source.py`
* Created `app/services/nyxcore.py` — async HTTP client for NyxCore Axiom API (`search_axiom`, `list_axiom_documents`)
* Updated `app/services/orchestrator.py`:
  - Extended `RetrievedChunk` with `authority` and `source_label` fields
  - Added `extra_chunks` parameter to `run_chat_turn` and `run_chat_turn_stream`
  - Added `_merge_chunks()` — sorts mandatory-authority chunks first, then by score
  - Updated `_build_messages()` to label chunks with `[MANDATORY]`, `[GUIDELINE]` etc.
* Updated `app/workers/ingest.py`:
  - Added `_validate_nyxcore()` — validates connection by listing documents, stores doc/chunk counts, marks READY
  - NYXCORE sources skip chunking/embedding entirely
* Updated `app/api/v1/chat.py`:
  - Added `_fetch_nyxcore_chunks()` — queries all active NYXCORE sources for the bot's profile, calls Axiom search, returns `RetrievedChunk` list
  - Wired into both streaming and non-streaming chat paths via `extra_chunks` parameter
* Updated `app/api/v1/sources.py`:
  - Added `_encrypt_config_token()` — encrypts `api_token` to `encrypted_token` using Fernet on source create/update
  - Added `_sanitize_config()` — replaces `encrypted_token` with `has_api_token: true` in API responses
* Updated `dashboard/index.html`:
  - Added "NyxCore" to type filter dropdown
  - Added "NyxCore Axiom" to source type selector in create modal
  - Added NyxCore-specific form: Base URL, API Token (password field), Result Limit, Authority checkboxes (Mandatory/Guideline/Informational), info banner
  - Added `createNyxcoreSource()` method with auto-validation polling
  - Added `toggleAuthority()` helper for checkbox state
  - Submit button shows "Connect" for NyxCore type
* Created `tests/test_nyxcore.py` — 8 tests: source CRUD, ingestion validation, chat with NyxCore context, Axiom search parsing, chunk merge ordering, token encryption roundtrip

## 3. The "Pain" Log (CRITICAL)
* **Tried:** Running tests without `ENCRYPTION_KEY` env var
* **Failed:** `RuntimeError: ENCRYPTION_KEY is not configured` — tests that encrypt/decrypt NyxCore tokens failed
* **Workaround:** Set `os.environ.setdefault("ENCRYPTION_KEY", "49tvPksgUV23soH7nAxE8cSLDp7BZ8FGId_VLlZb7Hs=")` at the top of `tests/test_nyxcore.py` before importing app modules. Run tests with `ENCRYPTION_KEY=... python3 -m pytest`.
* **Tried:** `pip3 install -e ".[dev]"` on macOS
* **Failed:** PEP 668 blocked system-wide install
* **Workaround:** Used `--break-system-packages` flag (no venv was set up in the project)
* *Note:* The project has no `.venv` — dependencies installed system-wide via pip3.

## 4. Active Variable State
* NyxCore BRbase project token provided by user: `nyx_ax_9ZXu8hnuZJVWhNhTxx6nxSowR-s8KLjSTXqFnjIKTWw` (project-scoped to `4c445cc1-ceb0-49c0-9e41-aaae419cb4bb`)
* NyxCore base URL: `https://nyxcore.cloud`
* NyxCore API endpoints: `POST /api/v1/rag/search`, `GET /api/v1/rag/documents`
* Test encryption key: `49tvPksgUV23soH7nAxE8cSLDp7BZ8FGId_VLlZb7Hs=`
* Deployment: GitHub Actions SSH to VPS at `/opt/minirag`, `docker compose up -d --build`
* Domain: `mini-rag.de` via Traefik reverse proxy

## 5. Immediate Next Steps
1. [ ] Create an actual NyxCore source via the dashboard UI on mini-rag.de and test the full flow end-to-end (create source → validate → chat → verify NyxCore context appears in responses)
2. [ ] Consider adding a "Test Connection" button in the dashboard that calls list_axiom_documents without creating the source
3. [ ] Consider adding NyxCore document list view in the dashboard (show what documents are available in the connected NyxCore project)
4. [ ] Add refresh schedule support for NyxCore sources (re-validate connection periodically)
5. [ ] Consider rate limit handling — NyxCore has 100 req/min per tenant; if multiple users chat simultaneously this could be hit
